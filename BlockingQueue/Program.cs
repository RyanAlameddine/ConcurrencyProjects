using System;
using System.Threading;

namespace BlockingQueue
{
    class Program
    {
        static void Main(string[] args)
        {
            BlockingQueue<int> blockingQ = new BlockingQueue<int>();

            Thread thr1 = new Thread(() =>
            {

            });
        }
    }
}
